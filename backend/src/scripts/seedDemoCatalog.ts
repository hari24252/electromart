/**
 * Demo catalog seeder — intentionally empty.
 * Set SEED_DEMO_CATALOG=true in .env only if you want to populate
 * the database with sample data for testing. Currently a no-op.
 */
export async function seedDemoCatalog(): Promise<void> {
  // No demo data. Add products via the Admin panel instead.
}
